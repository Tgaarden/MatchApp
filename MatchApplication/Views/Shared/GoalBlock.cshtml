@model MatchApplication.Models.Blocks.GoalBlock

@{
    Layout = null;
}

<div>
    <!--content link her-->
    <h4>MÅL!</h4>
    <span>@Html.PropertyFor(m => m.FirstName) i det @Html.PropertyFor(m => m.Minute) minuttet.</span>
    <p>@Html.PropertyFor(m => m.Description)</p>

</div>